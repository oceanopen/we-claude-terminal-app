{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "updater",
  "description": "auto-updater permissions scoped to the settings window (where AboutPage lives)",
  "windows": [
    "settings"
  ],
  "permissions": [
    "updater:default",
    "process:default"
  ]
}
